{
  "name": "relata-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "Relata: AI memory for the people you meet.",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
